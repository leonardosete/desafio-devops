apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-app-tembici.fullname" . }}
  labels:
    {{- include "flask-app-tembici.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-app-tembici.selectorLabels" . | nindent 4 }}
